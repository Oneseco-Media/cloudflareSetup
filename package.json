{
  "name": "workspace",
  "version": "0.0.0",
  "type": "module",
  "module": "src/index.ts",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250224.0",
    "@types/bun": "latest",
    "typescript": "^5.7.3",
    "vitest": "^3.0.7",
    "wrangler": "2.21.2"
  },
  "private": true,
  "scripts": {
    "start": "bun src/index.ts",
    "start:wrangler": "wrangler dev --local",
    "deploy": "wrangler publish",
    "test": "vitest",
    "start-persist": "wrangler dev --local --persist-to ./data",
    "replit-run-command": "bun src/index.ts"
  }
}
